<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Background color -->
    <stroke android:width="2dp" android:color="@color/black"/> <!-- Border color -->
    <corners android:radius="2dp"/> <!-- No rounded corners (perfect rectangle) -->
    <padding android:left="12dp" android:right="12dp" android:top="12dp" android:bottom="12dp"/>
</shape>
